//! WebRTC audio client entry point re-exporting core modules from `wffi`.

pub use wffi::{address, audio, call, config, resample};
pub use wffi::{CONFIGURATION, Configuration, DEFAULT_CONFIG_PATH, UserAddress};
