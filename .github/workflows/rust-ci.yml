name: Rust

on:
  push:

permissions:
  contents: read

jobs:
  ci-on-Unix:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1
      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
      - name: Install Nix
        uses: cachix/install-nix-action@v31.11.1
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      # cargo-build, cargo-test, cargo-check, and cargo-clippy
      - name: Run build
        run: nix develop -L --command sh -c 'cargo build --workspace'
      - name: Run tests
        run: nix develop -L --command sh -c 'cargo test --workspace'
      - name: Run check
        run: nix develop -L --command sh -c 'cargo check --workspace'
      - name: Run clippy
        run: nix develop -L --command sh -c 'cargo clippy --workspace'

  ci-on-Windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1
      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
      - name: Setup rustup
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.90.0
          components: clippy, rust-analyzer, rust-src

      # cargo-build, cargo-test, cargo-check, and cargo-clippy
      - name: Run build
        run: cargo build --workspace
      - name: Run tests
        run: cargo test --workspace
      - name: Run check
        run: cargo check --workspace
      - name: Run clippy
        run: cargo clippy --workspace
